## Session Start
On the first message of any new session:
1. Run `git pull origin main` to update the repository
2. Read and execute all steps in `INSTALL.md`
3. Only then proceed with the user's request